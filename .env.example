# Este arquivo é usado apenas para desenvolvimento local sem Docker
# Ao usar Docker Compose, as variáveis são configuradas automaticamente no docker-compose.yml

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=admin
DB_NAME=romaneio_db

# Application Configuration
PORT=3000
NODE_ENV=development

# Swagger Configuration
SWAGGER_ENABLED=true
